{
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true,
  },
  "include": [
    "source/server/**/*.ts"
  ],
  "exclude": [
    "source/server/node_modules",
    "source/server/dist",
  ]
}